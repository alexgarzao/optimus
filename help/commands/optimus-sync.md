---
name: optimus-sync
description: Sync all Optimus plugins — install new, update existing, remove orphaned
---

Run this script and show the full output to the user:

```bash
bash "${DROID_PLUGIN_ROOT}/scripts/sync-user-plugins.sh"
```

Do NOT add commentary. Just execute the script and display the output.
